import { ElementEditorDirective } from './element-editor.directive';

describe('ElementEditorDirective', () => {
  it('should create an instance', () => {
    // const directive = new ElementEditorDirective();
    // expect(directive).toBeTruthy();
  });
});
